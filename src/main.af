.needs <std>

import string, {print} from "String" under str;

types(T)
fn print(T value) {
    str.print(`{value}\n`);
    return;
};

fn main() {
    print(17);
    print("Hello, World!");
    print("Can we do it twice? Yes, we can!");
};
