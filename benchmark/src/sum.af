.needs <std>

import {printInt, printChar} from "io" under io;

int main(){
    mutable int sum = 0;
    for int i = 0; i < 100000; i++ {
        sum = sum + i;
    };
    io.printInt(sum);
    io.printChar('\n');
    return 0;
};
