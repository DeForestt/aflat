.needs "/std/head/std.af"
.needs "/std/head/asm.gs"

adr malloc(int size){
    adr out = brk(0);
    brk(out + size);
    return out;
};

Time newTime(adr count){
    Time this = malloc(15);
    this.amount = count;
    return this;
};