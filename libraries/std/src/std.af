.needs "/std/head/std.gs"
.needs "/std/head/asm.gs"

adr malloc(int size){
    adr out = brk(0);
    brk(out + size);
    return out;
};

Time newTime(adr count){
    Time this = malloc(16);
    this.amount = count;
    return this;
};