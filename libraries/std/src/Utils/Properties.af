.needs <std>

/*
 * Decorate a class method to be treated as a read-only calculated property.
 */
class Property {
    private const adr foo = foo;
    private const any context = context;

    Property init(const adr foo, const any context){
        return my;
    };

    any get() {
        return my.foo(my.context);
    };
};