.needs <std>

import Server from "HTTP/Server";
import HTTPMessage from "HTTP";
import string from "String";

class Middleware {
    const any<Server, HTTPMessage> implementation = implementation;
    
    Middleware init(const any<Server, HTTPMessage> implementation, * const Server server) {
        if server != NULL
            server.addMiddleware(my);
        return my;
    };

    string toString() {
        return "Middlewear";
    };
};